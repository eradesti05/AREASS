services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.prod     # pakai Dockerfile khusus prod (nginx static)
    networks:
      - areass-net
      - proxy

  backend:
    networks:
      - areass-net
      - proxy

  ml:
    networks:
      - areass-net
      - proxy 

networks:
  proxy:
    external: true