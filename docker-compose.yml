services:
  mongodb:
    image: mongo:7
    container_name: areass-mongo
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-secret}
      MONGO_INITDB_DATABASE: ${MONGO_DB:-areass}
    volumes:
      - mongo_data:/data/db
    networks:
      - areass-net

  backend:
    build: ./backend
    container_name: areass-backend
    restart: unless-stopped
    environment:
      PORT: 5000
      MONGODB_URI: mongodb://${MONGO_USER:-admin}:${MONGO_PASSWORD:-secret}@mongodb:27017/${MONGO_DB:-areass}?authSource=admin
      ML_API_URL: http://ml:8000
      JWT_SECRET: ${JWT_SECRET:-changeme}
    depends_on:
      - mongodb
    networks:
      - areass-net

  ml:
    build: ./ML
    container_name: areass-ml
    restart: unless-stopped
    networks:
      - areass-net

  frontend:
    build: ./frontend
    container_name: areass-frontend
    restart: unless-stopped
    depends_on:
      - backend
    networks:
      - areass-net

volumes:
  mongo_data:

networks:
  areass-net:
    driver: bridge